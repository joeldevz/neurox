# Generated by /setup — re-run to regenerate
version: 1
updated_at: "2026-06-19"

stack:
  runtime: go
  language: go
  framework: none
  test_runner: go-test
  architecture: modular

commands:
  test: "go test -tags fts5 ./..."
  build: "go build -tags fts5 ./..."
  lint: "go vet -tags fts5 ./..."

workflow:
  mode: interactive
  review_budget: 600

paths:
  conventions: CLAUDE.md
