version: 2

# GoReleaser is not used for builds (CGO cross-compilation is handled
# directly in .github/workflows/release.yml using native runners).
# This file is kept for reference and future use.

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^ci:"
