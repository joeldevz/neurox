version: 2

# GoReleaser is not used for builds currently.
# This file is kept for reference and future use.

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^ci:"
